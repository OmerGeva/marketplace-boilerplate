<div class="container text-center p-5">
  <h3>My Schedule</h3>
  <div class="row d-flex justify-content-center">
    <div class="col-8">
      <%= month_calendar do |date| %>
        <% @bookings.each do |booking| %>
          <%= date.strftime("%d") %>
          <p><%= booking.haircut.title if booking.date == date %></p>
        <% end %>
        <%=  %>
      <% end %>
    </div>
  </div>
</div>


